//device types
const DEVICES = {
    mobile: "mobile",
    desktop: "desktop"
}

const DESKTOP_CAMERA = {
    x: 0,
    y: 3,
    z: 3,
    xRotation: -15
}

const EMPTY_ID = "";

//UI constants
const HIDE_UI = "none";
const SHOW_UI = "flex";