//device types
const DEVICES = {
    mobile: "mobile",
    desktop: "desktop"
}

const DESKTOP_CAMERA = {
    x: 0,
    y: 8,
    z: 8,
    xRotation: -36
}

const EMPTY_ID = "";

//UI constants
const HIDE_UI = "none";
const SHOW_UI = "block";