//device types
const DEVICES = {
    mobile: "mobile",
    desktop: "desktop"
}

const DESKTOP_CAMERA = {
    x: 0,
    y: 1,
    z: 6,
    xRotation: 0
}

const EMPTY_ID = "";

//UI constants
const HIDE_UI = "none";
const SHOW_UI = "flex";